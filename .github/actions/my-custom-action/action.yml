name: "Simple custom action"
description: "This action write in the console the input value"
author: "Your Name"

inputs:
  data:
    description: "The input data to write in the console"
    required: false
    default: "<no data passed>"
  interval:
    description: "Millisecond to wait in the action"
    required: false
    default: 10000

runs:
  using: "node20"
  main: "index.js"
